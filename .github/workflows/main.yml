# name: Deploy Vite App to S3

# on:
#   push:
#     branches:
#       - main   # main 브랜치에 push 될 때 실행

# jobs:
#   build-and-deploy:
#     runs-on: ubuntu-latest

#     steps:
#       # 1. 레포 checkout
#       - name: Checkout repository
#         uses: actions/checkout@v3

#       # 2. Node.js 설정
#       - name: Set up Node.js
#         uses: actions/setup-node@v3
#         with:
#         //  node-version: 18

#       # 3. 패키지 설치
#       - name: Install dependencies
#         run: npm install

#       # 4. Vite 빌드 실행
#       - name: Build project
#         run: npm run build

#       # 5. AWS 인증
#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v2
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: ap-northeast-2 # 서울 리전 예시 // ${{ secrets.AWS_REGION }}

#       # 6. dist → S3 업로드
#       - name: Deploy to S3
#         run: aws s3 sync dist/ s3://your-bucket-name ${{ secrets.AWS_S3_BUCKET }}
